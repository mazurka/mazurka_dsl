
action() ->

testing(Var1, Var2) ->

testing (Var1, Var2) ->

read(PostID) ->
  #{
    favorites => [@users:read(Favorite) || Favorite <- Favorites]

    #|
    This is the length right here
    |#
    count => length(Favorites)
  }
